{
  "name": "nika-character-studio-vercel",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "test:unit": "node --test tests/*.test.js",
    "hash-password": "node scripts/hash-password.mjs",
    "generate-secret": "node scripts/generate-secret.mjs",
    "check-env": "node scripts/check-env.mjs",
    "dev": "vercel dev",
    "start": "vercel dev"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.0.2",
    "@vercel/blob": "^2.0.0",
    "@vercel/queue": "^0.2.0",
    "zod": "^4.1.12"
  },
  "devDependencies": {
    "vercel": "^48.10.2"
  }
}
